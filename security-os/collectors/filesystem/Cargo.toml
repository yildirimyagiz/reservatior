[package]
name = "security-os-collector-filesystem"
version = "0.1.0"
edition = "2021"

[dependencies]
security-os-core = { path = "../../core" }
tokio = { version = "1", features = ["full"] }
walkdir = "2"
sha2 = "0.10"
hostname = "0.4"
tracing = "0.1"
