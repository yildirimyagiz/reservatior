[package]
name = "security-os-collector-network"
version = "0.1.0"
edition = "2021"

[dependencies]
security-os-core = { path = "../../core" }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
hostname = "0.4"
serde_json = "1"
