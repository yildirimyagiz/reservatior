[package]
name = "security-os-engine-lateral_movement"
version = "0.1.0"
edition = "2021"

[dependencies]
security-os-core = { path = "../../core" }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
serde_json = "1"
dashmap = "6"

