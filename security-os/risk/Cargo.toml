[package]
name = "security-os-risk"
version = "0.1.0"
edition = "2021"

[dependencies]
security-os-core = { path = "../core" }
dashmap = "6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
tracing = "0.1"
