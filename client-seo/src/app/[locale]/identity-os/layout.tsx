import { IdentityLayout } from "@/components/spa-layouts/identity_os/IdentityLayout";

export default function IdentityOSLayout({ children }: { children: React.ReactNode }) {
  return <IdentityLayout>{children}</IdentityLayout>;
}
