import { DocumentLayout } from "@/components/spa-layouts/document_os/DocumentLayout";

export default function DocumentOSLayout({ children }: { children: React.ReactNode }) {
  return <DocumentLayout>{children}</DocumentLayout>;
}
