import { NotificationLayout } from "@/components/spa-layouts/notification_os/NotificationLayout";

export default function NotificationOSLayout({ children }: { children: React.ReactNode }) {
  return <NotificationLayout>{children}</NotificationLayout>;
}
