import { LocalizationLayout } from "@/components/spa-layouts/localization_os/LocalizationLayout";

export default function LocalizationOSLayout({ children }: { children: React.ReactNode }) {
  return <LocalizationLayout>{children}</LocalizationLayout>;
}
