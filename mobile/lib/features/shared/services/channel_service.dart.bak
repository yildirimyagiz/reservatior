import 'package:dio/dio.dart';
import '../../../core/network/dio_client.dart';
import '../../gen_models/models_library.dart';

class ChannelService {
  final DioClient _dioClient;
  ChannelService(this._dioClient);

  // ── Get by ID ──
  Future<Channel> getById(String id) async {
    if (id.isEmpty) throw ArgumentError('ID cannot be empty');
    try {
      final r = await _dioClient.get('/channel/$id');
      return Channel.fromJson(r.data['data']);
    } on DioException catch (e) { throw _err(e); }
  }

  // ── Get all ──
  Future<List<Channel>> getAll({
    int page = 1, int limit = 20,
    Map<String, dynamic>? filters,
  }) async {
    try {
      final q = <String, dynamic>{'page': page.toString(), 'limit': limit.toString()};
      if (filters != null) q.addAll(filters);
      final r = await _dioClient.get('/channel', queryParameters: q);
      return (r.data['data'] as List).map((j) => Channel.fromJson(j)).toList();
    } on DioException catch (e) { throw _err(e); }
  }

  // ── Get with filters ──
  Future<List<Channel>> getWithFilters({
    String? cuid,
    String? name,
    ChannelType? type,
    ChannelCategory? category,
    String? description,
  }) async {
    final filters = <String, dynamic>{};
    if (cuid != null) filters['cuid'] = cuid;
    if (name != null) filters['name'] = name;
    if (type != null) filters['type'] = type.toString();
    if (category != null) filters['category'] = category.toString();
    if (description != null) filters['description'] = description;
    return getAll(filters: filters);
  }

  // ── Create ──
  Future<Channel> create(Channel channel) async {
    if (channel.name == null || channel.name!.isEmpty) {
      throw ArgumentError('name is required');
    }
    if (channel.type == null || channel.type!.isEmpty) {
      throw ArgumentError('type is required');
    }
    try {
      final r = await _dioClient.post('/channel', data: channel.toJson());
      return Channel.fromJson(r.data['data']);
    } on DioException catch (e) { throw _err(e); }
  }

  // ── Update ──
  Future<Channel> update(String id, Channel channel) async {
    if (id.isEmpty) throw ArgumentError('ID cannot be empty');
    try {
      final r = await _dioClient.put('/channel/$id', data: channel.toJson());
      return Channel.fromJson(r.data['data']);
    } on DioException catch (e) { throw _err(e); }
  }

  // ── Delete ──
  Future<void> delete(String id) async {
    if (id.isEmpty) throw ArgumentError('ID cannot be empty');
    try {
      await _dioClient.delete('/channel/$id');
    } on DioException catch (e) { throw _err(e); }
  }

  Exception _err(DioException e) {
    switch (e.type) {
      case DioExceptionType.connectionTimeout:
        return Exception('Connection timeout. Check your internet connection.');
      case DioExceptionType.badResponse:
        final msg = e.response?.data?['message'] ?? 'Server error';
        return Exception('Server error: $msg');
      case DioExceptionType.connectionError:
        return Exception('Network error. Check your internet connection.');
      default:
        return Exception('Request failed: ${e.message}');
    }
  }
}